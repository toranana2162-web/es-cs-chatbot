# 操作手順書（オペレーター向け）

対象読者: BOTANICAカスタマーサポートを担当するオペレーター（admin / operator）。

## 1. ログイン

1. `/operator/login` へアクセスする。
2. 管理者から発行されたメールアドレス・パスワードを入力し「ログイン」を押す。
3. 認証に成功すると会話一覧画面（`/operator`）へ遷移する。
4. パスワードを忘れた場合は管理者に再登録を依頼する（本MVPにはパスワードリセット画面がない。D-010）。

未登録のメールアドレスや誤ったパスワードの場合はフォーム下にエラーメッセージが表示される。

## 2. 会話一覧の見方

会話一覧には以下が表示される。

- **ステータス**（StatusBadge）
  - `ai_handling`：AIが対応中
  - `waiting_operator`：人間の対応待ち
  - `operator_handling`：オペレーターが対応中
  - `closed`：完了
- **カテゴリ**（CategoryBadge）：`inventory` / `product` / `shipping` / `return` / `other`。AIが判定できなかった場合は表示なし。
- **未読インジケータ**：直近のメッセージが未確認の会話に表示される。
- **状態フィルタ**：ステータスで一覧を絞り込める。

一覧はRealtimeで自動更新されるため、手動リロードは不要。

## 3. 会話への対応

### 3.1 担当を開始する

1. 対応が必要な会話（`waiting_operator`）を開く。
2. 「担当開始」ボタンを押す。
3. 成功すると自分がその会話の担当者になり、ステータスが `operator_handling` に変わる。

**注意（D-008）**: 複数オペレーターが同時に「担当開始」を押した場合、先にDB更新に成功した1名だけが担当者になる。後から押した側にはエラーが表示され、担当できない（正常な仕様）。

### 3.2 返信する

- 自分が担当している会話（`operator_handling`かつ自分が担当者）のみ、返信フォームが表示される。
- 他のオペレーターが担当中の会話には返信できない（「◯◯が対応中です」と表示され、返信フォームは出ない）。
- メッセージは1,000文字まで。Enterで送信、Shift+Enterで改行。

### 3.3 会話を完了する

- 返信が完了し対応が終わったら「会話完了」ボタンを押す。
- ステータスが `closed` になり、顧客側の入力欄も無効化される。
- 完了した会話は一覧から削除されない（MVPでは会話削除機能なし。D-009）。再度開いて内容を確認することは可能。

## 4. エスカレーション理由の見方

会話詳細画面には、AIが人間対応へ引き継いだ理由が表示される（該当する場合）。

| 表示ラベル | 意味 |
|---|---|
| 顧客からの希望 | 顧客が人間対応を希望した |
| 関連FAQなし | 該当するFAQが見つからなかった |
| FAQ類似度不足 | FAQは見つかったが類似度が閾値未満だった |
| 個別注文確認が必要 | 注文内容など個別確認が必要な質問だった |
| 返金・決済関連 | 返金・決済・配送事故など個別判断が必要 |
| クレーム | クレーム・強い不満・法的要求を含む |
| AI判断不能 | AIがFAQだけでは回答できないと判断した |
| AI API障害 | Claude APIの呼び出し自体に失敗した |

「営業時間外」バッジが表示されている会話は、営業時間外（平日10:00〜18:00以外）に受け付けたことを示す。対応の優先度判断に利用する。

## 5. よくある操作ミスと対処

| 症状 | 原因 | 対処 |
|---|---|---|
| 「担当開始」を押してもエラーになる | 他のオペレーターが先に担当開始した | 一覧を確認し、担当済みの会話として扱う |
| 返信フォームが表示されない | 自分が担当者でない、または会話が完了済み | 担当者・ステータスを確認する |
| ログインできない | メールアドレス／パスワード誤り、未登録 | 管理者に確認・再登録を依頼する |

より技術的な障害（画面が真っ白になる等）は [TROUBLESHOOTING.md](TROUBLESHOOTING.md) を参照。
