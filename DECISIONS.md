# 設計判断記録

## D-001 Shopify API連携をPhase 2へ送る
### 判断
MVPではShopify APIを実装せず、FAQベースの回答に集中する。

### 理由
- 2週間で確実に納品するため
- Shopify側の権限・在庫仕様・配送仕様の確認に時間がかかる
- まずAI回答と人間引き継ぎの価値を検証する

## D-002 分析ダッシュボードをPhase 2へ送る
### 判断
MVPでは会話履歴を保存するが、分析画面は作らない。

### 理由
- 分析には十分な会話データが必要
- 最重要課題は返信遅延と離脱
- 保存データを後から分析へ利用できる

## D-003 顧客識別にSupabase匿名認証を使う
### 判断
ブラウザ保存のsession_idだけではなく、Supabase匿名認証で顧客を識別する。

### 理由
- RLSを安全に構成しやすい
- 他人の会話を閲覧できる事故を防ぎやすい

## D-004 共通基盤はmainで先に確定する
### 判断
DB、RLS、共通型、環境変数、基本ディレクトリはWorktree分割前にmainで作る。

### 理由
- 3ブランチが同じファイルを変更する競合を減らす
- ステータス名や型の不一致を防ぐ

## D-005 3領域をWorktreeで分離する
### 判断
顧客UI、AIバックエンド、管理画面を別ブランチ・別Worktreeで開発する。

### 理由
- 同時並行で進められる
- 担当範囲が明確になる
- Claude Codeの作業コンテキストが混ざりにくい

## D-006 営業時間外を専用ステータスではなくフラグで管理する
### 判断
会話ステータスは`waiting_operator`を使用し、営業時間外かどうかは`is_after_hours`で区別する。

### 理由
- 会話ステータスの種類を増やしすぎない
- 管理画面で営業時間内と時間外を区別できる
- 営業時間外でも、業務上は同じオペレーター待ちである

## D-007 Claude API障害時は人間対応へ切り替える
### 判断
Claude APIの呼び出しに失敗した場合は、会話を`waiting_operator`へ変更する。

### 理由
- 顧客メッセージを失わない
- AI障害によって会話が止まることを防ぐ
- 顧客に誤った回答を返すより安全である

## D-008 オペレーターの担当開始は先着1名とする
### 判断
同じ会話へ複数のオペレーターが同時に担当開始した場合は、最初にDB更新へ成功した1名だけを担当者とする。

### 理由
- 二重返信を防ぐ
- 担当者の上書きを防ぐ
- 2名体制のMVPとして理解しやすい

## D-009 会話の物理削除機能をMVPへ含めない
### 判断
MVPでは管理画面から会話を削除できない。

### 理由
- 誤削除を防ぐ
- 会話履歴を品質改善へ利用できる
- 個人情報削除や保存期間は別途運用要件として決定する

## D-010 オペレーター登録は管理者が行う
### 判断
MVPでは一般利用者向けのオペレーター登録画面を作らない。
初期オペレーター2名は、Supabase Authとoperator_profilesへ管理者が登録する。

### 理由
- 不正なオペレーター登録を防ぐ
- MVPで招待管理画面を作る必要がない
- 利用者が2名に限定されている

## D-011 Embeddingモデルの選定
### 判断
- プロバイダー：OpenAI
- モデル名：text-embedding-3-small
- ベクトル次元数：1536（デフォルト値のまま短縮しない）
- FAQ検索件数：上位3件
- 類似度の初期閾値：0.75（コサイン類似度）

### 選定理由
- FAQ18件〜将来500件程度の規模では、text-embedding-3-large（3072次元）の精度優位はほぼ体感できず、次元数だけが倍増しpgvectorインデックスが不必要に重くなる
- Supabase pgvectorとの連携実績・ドキュメントが最も豊富で、2週間納品のスケジュール（D-001）において実装リスクが低い
- 言い換え・誤字を含む短い質問文の意味検索において日本語性能が実用上十分である
- 最安クラスであり、月間500件規模・将来500件のFAQ規模でもコストは無視できる水準
- 次元数1536はpgvectorのインデックス性能上も扱いやすく、将来FAQが増えてもインデックス再設計が不要

### 注意
- 類似度閾値0.75は初期値であり、実装後にTEST_PLAN.mdのFAQ検索単体テスト（完全一致・言い換え・誤字・無関係な質問・複数FAQに近い質問）の結果を見ながら調整する
- モデルを変更してベクトル次元数が変わる場合、DBのembedding列変更と全FAQの再Embeddingが必要になる

## D-012 escalated_reasonをコード値のenumで管理する
### 判断
`escalated_reason`は自由入力のテキストではなく、以下8種類の固定コード値（EscalationReason）のいずれかとする。

- `customer_request`：顧客が人間または担当者による対応を希望した
- `faq_not_found`：関連FAQが取得できなかった
- `low_similarity`：FAQ検索結果が類似度の閾値を下回った
- `order_specific`：個別の注文情報を確認しなければ回答できない
- `refund_or_payment_issue`：返金、決済、配送事故など個別判断が必要
- `complaint`：クレーム、強い不満、法的要求を含む
- `ai_uncertain`：ClaudeがFAQだけでは回答できないと判定した
- `ai_api_error`：Claude APIの呼び出しに失敗した

### 理由
- 管理画面でエスカレーション理由を集計・フィルタしやすくする
- Worktreeごとに異なる自由文言の理由が混在することを防ぐ
- FR-06の8条件と一対一で対応させ、実装時の判断基準を明確にする

## D-013 営業時間判定は現在時刻を引数で受け取る純粋関数にする
### 判断
営業時間外判定のロジックは、関数内部で現在時刻を取得せず、呼び出し側から現在時刻を引数として渡す純粋関数として実装する。

```ts
function isAfterHours(now: Date, holidays: Date[]): boolean
```

本番コードの呼び出し側では`isAfterHours(new Date(), holidays)`のように実時刻を渡す。テストコードでは任意のDateを渡すことで、平日・土日・休業日・時刻境界（10:00/18:00ちょうど等）のケースを実時刻に依存せず検証する。

### 理由
- モックライブラリやシステム時計の差し替えが不要で、テストコードが単純になる
- 純粋関数であるため単体テストが容易で、並列実行時の時刻状態共有によるテストの汚染がない
- test-conversations.jsonの`business_context`（`business_hours` / `after_hours`）は、テスト実行時にこの`now`引数へ変換して渡す運用とする