# 要件定義書

## 1. 背景
現在の問い合わせ窓口はフォームのみで、返信まで1〜2日かかっている。問い合わせ待ちによる顧客離脱を減らすため、即時対応できるCSチャットを導入する。

月間問い合わせ件数は約500件。内訳は以下を想定する。

| カテゴリ | 比率 |
|---|---:|
| 在庫確認 | 30% |
| 商品質問 | 25% |
| 配送状況 | 20% |
| 返品・交換 | 15% |
| その他 | 10% |

オペレーターは2名、営業時間は10:00〜18:00。営業時間外はAIのみが一次対応する。

## 2. 目的
FAQで回答可能な問い合わせをAIが即時処理し、判断が難しい問い合わせのみ人間へ引き継ぐことで、返信遅延・顧客離脱・オペレーター負担を減らす。

## 3. MVPに含めるもの
- 顧客向けチャットウィジェット
- FAQベースのAI自動応答
- FAQ検索
- 問い合わせカテゴリ判定
- 回答不能時のオペレーターへのエスカレーション
- オペレーター管理画面
- オペレーターから顧客への返信
- 会話履歴保存
- Supabase Realtimeによるリアルタイム反映
- オペレーター認証
- 顧客とオペレーターの権限分離
- 営業時間内・時間外の動作切り替え
- 基本的なブランド表示

## 4. Phase 2へ回すもの
- Shopify APIによる在庫照会
- Shopify APIによる配送状況照会
- 感情分析
- 分析ダッシュボード
- 商品レコメンド
- 高度なブランドカスタマイズ
- 多言語対応
- 画像・ファイル添付
- 決済・返金処理
- クーポン自動発行

## 5. ユーザー種別

### 顧客
- ECサイト訪問者
- ログイン操作は不要
- 自分の会話のみ閲覧可能
- 質問を送信できる
- AIまたはオペレーターの返信を受信できる

### オペレーター
- Supabase Authでログイン
- 全会話を閲覧できる
- 対応待ち会話を確認できる
- 担当者として会話を引き受けられる
- 顧客へ返信できる
- 会話を完了できる

## 6. 機能要件

### FR-01 チャット開始
顧客がウィジェットを開いた際、匿名セッションを発行して会話を開始できる。

### FR-02 メッセージ送信
顧客はテキストメッセージを送信できる。

### FR-03 FAQ検索
顧客メッセージに関連するFAQを検索し、AIへ根拠として渡す。

### FR-04 AI自動応答
FAQに十分な根拠がある場合、AIが自然な日本語で回答する。

### FR-05 回答抑制
FAQに根拠がない場合、AIは事実を作らず、人間確認へ切り替える。

### FR-06 エスカレーション

以下のいずれかに該当した場合、会話ステータスを`waiting_operator`へ変更する。それぞれの条件は`escalated_reason`へ保存する固定のコード値（EscalationReason）に対応する。

- 顧客が人間または担当者による対応を希望した（`customer_request`）
- 関連FAQが取得できなかった（`faq_not_found`）
- FAQ検索結果が設定した類似度の閾値を下回った（`low_similarity`）
- 個別の注文情報を確認しなければ回答できない（`order_specific`）
- 返金、決済、配送事故など個別判断が必要（`refund_or_payment_issue`）
- クレーム、強い不満、法的要求を含む（`complaint`）
- ClaudeがFAQだけでは回答できないと判定した（`ai_uncertain`）
- Claude APIの呼び出しに失敗した（`ai_api_error`）

エスカレーション時は以下を実行する。

- 該当する`escalated_reason`を保存する
- 顧客へ担当者が確認することを案内する
- 営業時間外の場合は翌営業日の対応になることを案内する

### FR-07 管理画面
オペレーターは会話一覧、会話詳細、状態、カテゴリを確認できる。

### FR-08 オペレーター返信
オペレーターの返信は顧客側へリアルタイム表示される。

### FR-09 ステータス管理
会話は以下の状態を持つ。
- `ai_handling`
- `waiting_operator`
- `operator_handling`
- `closed`

### FR-10 営業時間外

営業時間は日本時間の平日10:00〜18:00とする。
土曜日、日曜日、および`business_holidays`テーブルに登録された祝日・休業日は営業時間外として扱う。曜日判定はアプリ側で行い、祝日・特別休業日はDBで管理する。
営業時間外もAIは通常どおりFAQ回答を行う。
人間対応が必要になった場合は以下を実行する。
- conversations.statusを`waiting_operator`へ変更
- conversations.is_after_hoursをtrueにする
- 顧客へ翌営業日に担当者が確認することを案内する
- 管理画面で営業時間外の待機案件であることを表示する
営業時間内に発生した待機案件では、is_after_hoursをfalseとする。
営業時間外判定は現在時刻を関数内部で取得せず、呼び出し側から引数として受け取る純粋関数として実装する（D-013）。

### FR-11 会話履歴保存
顧客・AI・オペレーターの全メッセージを保存する。

### FR-12 カテゴリ管理
- `inventory`
- `product`
- `shipping`
- `return`
- `other`

### FR-13 AI API障害時の処理
Claude APIのタイムアウト、レート制限、通信障害、レスポンス解析失敗が発生した場合、顧客メッセージを失わない。
以下を実行する。
- エラー内容をサーバーログへ記録する
- conversations.statusを`waiting_operator`へ変更する
- escalated_reasonへ`ai_api_error`を保存する
- 顧客へ一時的に自動回答できないことを案内する
- APIキーや顧客の秘密情報はログへ記録しない

### FR-14 オペレーターの担当開始
オペレーターが「対応を開始する」を押した場合、まだ担当者が設定されていない会話に限り担当できる。
同じ会話に対して複数のオペレーターが同時に担当開始を実行した場合は、最初にDB更新へ成功した1名だけを担当者とする。
後から操作したオペレーターには、すでに他の担当者が対応中であることを表示する。

### FR-15 サポート対象外の質問
ECサイトの問い合わせと無関係な質問には回答しない。
顧客へサポート対象外であることを案内し、原則としてオペレーターへエスカレーションしない。
対象外質問が繰り返された場合は、会話を終了できる。

### FR-16 メッセージ入力制限
顧客およびオペレーターが1回に送信できるメッセージは最大1,000文字とする。
空文字、空白のみのメッセージ、最大文字数を超えるメッセージは送信できない。
顧客の連続送信は、1ユーザーにつき1分間に10回までを目安として制限する。

## 7. 非機能要件

### 性能
- 通常のメッセージ表示は数秒以内
- Realtime反映は原則即時
- 月間500件程度を安定して処理

### 可用性
- AI応答失敗時も顧客メッセージを失わない
- 外部サービス障害時はエラー表示と再試行導線を用意する

### 保守性
- 顧客UI・AIバックエンド・管理画面を分離する
- 共通型、ステータス、カテゴリは統一する
- DB変更はマイグレーションで管理する

### セキュリティ
- APIキーをクライアントへ公開しない
- RLSを必須とする
- 顧客は自分の会話のみ閲覧可能
- 管理画面は権限を持つオペレーターのみ利用可能

## 8. 受け入れ条件
- FAQにある質問へAIが回答できる
- FAQにない質問で事実を作らずエスカレーションできる
- オペレーター返信が顧客画面へリアルタイム表示される
- 顧客Aから顧客Bの会話を閲覧できない
- 営業時間外の人間対応案件を待機状態として保存できる
